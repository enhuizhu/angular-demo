import { TearOutWindowDirective } from './tear-out-window.directive';

describe('TearOutWindowDirective', () => {
  it('should create an instance', () => {
    // const directive = new TearOutWindowDirective();
    // expect(directive).toBeTruthy();
  });
});

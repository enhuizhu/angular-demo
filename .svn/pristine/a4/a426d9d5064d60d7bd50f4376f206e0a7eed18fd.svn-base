import { TestBed, inject } from '@angular/core/testing';

import { GmexService } from './gmex.service';

describe('GmexService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GmexService]
    });
  });

  // it('should be created', inject([GmexService], (service: GmexService) => {
  //   expect(service).toBeTruthy();
  // }));
});
